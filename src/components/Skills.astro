---
const skillCategories = [
  {
    title: "Languages",
    skills: ["Python", "TypeScript", "JavaScript", "C++", "SQL", "HTML/CSS"],
  },
  {
    title: "Frontend",
    skills: ["React", "Vite", "Streamlit", "React Router"],
  },
  {
    title: "AI / Machine Learning",
    skills: ["PyTorch", "TensorFlow", "Keras", "Hugging Face", "OpenAI", "MediaPipe", "OpenCV", "FinBERT"],
  },
  {
    title: "Backend & Data",
    skills: ["RESTful APIs", "FastAPI", "Flask", "PostgreSQL", "SQLite", "ChromaDB", "Supabase", "Pandas"],
  },
  {
    title: "Tools & Deployment",
    skills: ["Git", "Vercel", "Render"],
  },
];

function toFileName(name: string): string {
  return name
    .toLowerCase()
    .replace(/\+\+/g, "-plus-plus")
    .replace(/\//g, "-")
    .replace(/\s+/g, "-");
}
---

<section class="skills" id="skills">
  <div class="container">
    <div class="section-label reveal"><span class="idx">04 /</span> Skills</div>
    <h2 class="section-title reveal reveal-delay-1">Tech Stack</h2>
    <p class="section-subtitle reveal reveal-delay-2">
      Technologies and tools I work with to bring ideas to life.
    </p>

    <div class="skills-grid">
      {skillCategories.map((cat, i) => (
        <div class={`skill-category reveal reveal-delay-${(i % 4) + 1}`}>
          <h3 class="category-title">{cat.title}</h3>
          <div class="skill-tags">
            {cat.skills.map(skill => (
              <span class="skill-tag">
                <img
                  class="skill-icon"
                  src={`/icons/skills/${toFileName(skill)}.svg`}
                  alt=""
                  width="14"
                  height="14"
                  loading="lazy"
                />
                {skill}
              </span>
            ))}
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .skills-grid {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 1.75rem;
    margin-top: 2.75rem;
  }

  .skill-category {
    padding: 0;
  }

  .category-title {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    font-weight: 600;
    color: var(--accent);
    margin-bottom: 1.1rem;
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }

  .skill-tags {
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
  }

  .skill-tag {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.83rem;
    color: var(--text-secondary);
    transition: color var(--transition-fast);
  }

  .skill-tag:hover {
    color: var(--text-primary);
  }

  .skill-icon {
    width: 14px;
    height: 14px;
    flex-shrink: 0;
    filter: invert(40%) sepia(0%) saturate(0%) brightness(90%);
    transition: filter var(--transition-fast);
    opacity: 0.6;
  }

  .skill-tag:hover .skill-icon {
    filter: invert(70%) sepia(0%) saturate(0%) brightness(120%);
    opacity: 0.9;
  }

  @media (max-width: 900px) {
    .skills-grid {
      grid-template-columns: repeat(3, 1fr);
      gap: 2rem;
    }
  }

  @media (max-width: 640px) {
    .skills-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
</style>
